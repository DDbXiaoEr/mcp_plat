# ============================================================
# audit-log-server 配置文件
# 复制为 audit_log_server.yml 后启动
# ============================================================

# gRPC 监听地址
grpc_addr: "127.0.0.1:9091"

# 审计日志数据库（与主程序 config.yaml 中 audit_log_db 共用）
database:
  type: postgres
  postgres:
    host: localhost
    port: 5432
    user: root
    password: 123456
    dbname: mcp_platform
