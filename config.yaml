database:
  # 二选一，取消对应注释即可
  # -------- SQLite --------
  # type: sqlite
  # sqlite:
  #   path: data.db
  #   max_idle_conns: 10
  #   max_open_conns: 100
  #   journal_mode: WAL
  #   busy_timeout: 5000

  # -------- PostgreSQL --------
  type: postgres
  postgres:
    host: localhost
    port: 5432
    user: root
    password: "123456"
    dbname: mcp_platform

jwt_secret: mcp-platform-secret-key
server_port: 8080

admin:
  username: admin
  password: admin123
